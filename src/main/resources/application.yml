
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/flowery?serverTimezone=Asia/Seoul
    username: root
    password: 003674

  jpa:
    show-sql: true
    hibernate.ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

server:
  port: 8002